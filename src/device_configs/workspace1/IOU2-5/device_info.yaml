---
hostname: "IOU2-5" # Hostname of device

config: "5" # Path to configuration file of device

version: "" # Version of running operating system

l3_protocols:    # L3 protocols which are used not only available but literally used and enabled

  - ipv4
  - ipv6
vendor: "cisco" # Manufacturer of device. Same directory name has to be created inside directory "modules", where all modules for this vendor are stored

os: "" # Operating system. Same directory name has to be created inside directory "modules" and subdirectory specified in variable "vendor" 

facility: "l3sw" # Type of device [r(router), l3sw(L3 switch), l2sw(L2 switch)]

facility_layer: "collapsed_distribution_access" # Type of layer where facility is installed [core, distribution, access, collapsed all, collapsed core distribution, collapsed distribution access]

exclude_modules: [] # Exclude modules which are specified in file "modules_by_facility_layer.yaml" for specific "facility_layer" you do not want to be used

include_modules: [] # Include modules which are not part of specific "facility_layer" in file "modules_by_facility_layer.yaml" and you want to use them.

interfaces:    # All available interfaces, roles of interfaces can be specified, roles such as "access" or "trunk" are assigned automatically according to config and more than one type can be assigned to port Roles: [access, trunk, wan, toinet, access-datacenter, wlan, to_distribution_layer, to_core_layer, unused, none] Roles: [access, trunk, uplink, access-datacenter, wlan, shutdown, noip, unused, subinterface, portchannel, none, svi] downlink -passive interface ? 

  Loopback0:
    - noip
  Port-channel1:
    - trunk
    - noip
    - port-channel
  Ethernet0/0: []
  Ethernet0/1:
    - access
    - noip
  Ethernet0/2:
    - noip
    - access
  Ethernet0/3:
    - noip
    - access
  Ethernet1/0:
    - noip
    - access
  Ethernet1/1:
    - access
    - noip
  Ethernet1/2:
    - access
    - noip
  Ethernet1/3:
    - noip
    - access
  Ethernet2/0:
    - trunk
    - noip
  Ethernet2/1:
    - trunk
    - noip
  Ethernet2/2:
    - noip
    - access
  Ethernet2/3:
    - noip
    - access
  Ethernet3/0:
    - noip
    - access
  Ethernet3/1:
    - noip
    - channel-group1
  Ethernet3/2:
    - trunk
    - noip
    - channel-group1
  Ethernet3/3:
    - noip
    - access
  Serial4/0:
    - shutdown
    - noip
  Serial4/1:
    - shutdown
    - noip
  Serial4/2:
    - shutdown
    - noip
  Serial4/3:
    - shutdown
    - noip
  Serial5/0:
    - shutdown
    - noip
  Serial5/1:
    - shutdown
    - noip
  Serial5/2:
    - shutdown
    - noip
  Serial5/3:
    - shutdown
    - noip
  Vlan1:
    - shutdown
    - svi
    - noip
  Vlan100:
    - svi
    - noip
  Vlan200:
    - svi
    - noip
enabled_functions: [] # Enabled functions such as eigrp, rip, ospf, bgp, vrrp, glbp, etc.

input_config_hash: "10b09a99888cd890f6d8bb5b9b220c53092b0579" # SHA1 hash of input configuration of device

fix_hash: "" # SHA1 hash of current fix configuration 
...
