---
hostname: "SW1_ACCESS" # Hostname of device

config: "SW1_ACCESS_conf.txt"   # Path to configuration file of device

version: ""             # Version of running operating system

l3_protocols: # L3 protocols which are used not only available but literally used and enabled
  - ipv4
  - ipv6
vendor: "cisco" # Manufacturer of device.Same directory name has to be created inside directory "Modules", where all modules for this vendor are stored

os: ""    # Operating system

facility: "l3sw" # Type of device [r(router), l3sw(L3 switch), l2sw(L2 switch)]

facility_layer: "core"   # Type of layer where facility is installed [core/edge, distribution, access, collapsed all, collapsed distribution access, collapsed core distribution]

exclude_modules: [] # Exclude modules which are specified in file "modules_by_facility.yaml" for specific "facility-layer" you do not want to be used
include_modules: [] # Include modules which are not part of  specific "facility-layer" in file "modules_by_facility.yaml" and you want to use them.
interfaces: # All available interfaces, roles of interfaces can be specified, roles such as "access" or "trunk" are assigned automatically according to config and more than one type can be assigned to port Roles: [access, trunk, wan, toinet, access-datacenter, wlan, to_distribution_layer, to_core_layer, unused, none] Roles: [access, trunk, uplink, access-datacenter, wlan, shutdown, noip, unused, subinterface, portchannel, none, svi] downlink -passive interface ? 
  Port-channel1:
    - trunk
    - noip
  Port-channel2:
    - trunk
    - noip
  Port-channel4:
    - trunk
    - noip
  Ethernet0/0:
    - trunk
    - noip
    - port-channel1
  Ethernet0/1:
    - trunk
    - noip
    - port-channel1
  Ethernet0/2:
    - trunk
    - noip
    - port-channel2
  Ethernet0/3:
    - trunk
    - noip
    - port-channel2
  Ethernet1/0:
    - access
    - shutdown
    - noip
  Ethernet1/1:
    - access
    - shutdown
    - noip
  Ethernet1/2:
    - access
    - shutdown
    - noip
  Ethernet1/3:
    - access
    - shutdown
    - noip
  Ethernet2/0:
    - trunk
    - noip
    - port-channel4
  Ethernet2/1:
    - trunk
    - noip
    - port-channel4
  Ethernet2/2:
    - trunk
    - noip
  Ethernet2/3:
    - noip
  Ethernet2/3.10:
    - noip
    - subinterface
  Ethernet2/3.20:
    - noip
    - subinterface
  Ethernet3/0:
    - access
    - noip
  Ethernet3/1:
    - access
    - shutdown
    - noip
  Ethernet3/2:
    - access
    - shutdown
    - noip
  Ethernet3/3:
    - access
    - shutdown
    - noip
  Serial4/0:
    - shutdown
    - noip
  Serial4/1:
    - shutdown
    - noip
  Serial4/2:
    - shutdown
    - noip
  Serial4/3:
    - shutdown
    - noip
  Serial5/0:
    - shutdown
    - noip
  Serial5/1:
    - shutdown
    - noip
  Serial5/2:
    - shutdown
    - noip
  Serial5/3:
    - shutdown
    - noip
  Vlan1:
    - shutdown
    - svi
    - noip
  Vlan200:
    - svi
routing_protocols: # Enabled routing protocols on device routing_protocols: [rip,ospf,eigrp,bgp]
  - rip
  - eigrp
input_config_hash: "a9f640e83eb739de322e2033c413600082153918" # SHA1 hash of input configuration of device

fix_hash: ""                                         # SHA1 hash of current fix configuration 
...
